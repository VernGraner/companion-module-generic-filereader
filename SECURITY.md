# Security

Do not commit passwords, API keys, access tokens, private keys, Companion
configuration exports, or files read by this module.

If a credential is accidentally committed, revoke or rotate it immediately
before removing it from Git history.
